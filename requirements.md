# Real-Estate Authorization System Requirements

## 1. 概要 (Overview)
本ドキュメントは、不動産管理プラットフォーム（real-estate）における認証・認可システムの要件を定義する。
本システムは、ユーザーの身元を検証（認証）し、役割（ロール）に基づいてリソースへのアクセスを制御（認可）することを目的とする。

## 2. スコープ (Scope)
### 2.1 In Scope
- メールアドレスとパスワードによるユーザー認証
- JWT (JSON Web Token) を用いたステートレスな認証状態管理
- アクセストークンとリフレッシュトークンによるセッション管理
- ロールベースアクセス制御 (RBAC) ミドルウェアの実装
- 保護された API エンドポイントの実装例
- フロントエンド（React）におけるログインフローの実装例

### 2.2 Out of Scope
- OAuth (Google, Apple 等) によるソーシャルログイン
- 多要素認証 (MFA)
- UI デザインの作り込み（機能確認用UIのみ）
- 本番環境へのデプロイ作業

## 3. ユーザーロール (User Roles)
| ロール名 | 説明 |
| :--- | :--- |
| **Admin** | システム管理者。全ての操作が可能。 |
| **Agent** | 不動産会社スタッフ。物件管理・契約管理が可能。 |
| **Owner** | 物件オーナー。自身の物件情報の閲覧・管理が可能。 |
| **Customer** | 一般利用者。物件閲覧、自身のプロフィール管理が可能。 |
| **Pending_Agent** | 未承認スタッフ。機能制限あり（承認待ち状態）。 |

## 4. 機能要件 (Functional Requirements)

### FR-01: ユーザー認証 (Authentication)
**User Story:** As a ユーザー, I want メールとパスワードでログインしたい, so that システムを利用開始できる。

**Acceptance Criteria (EARS):**
- **WHEN** 登録済みのメールアドレスと正しいパスワードが送信された場合、 **THEN** システムは有効なアクセストークンとリフレッシュトークンを返却しなければならない。
- **WHEN** 未登録のメールアドレスまたは誤ったパスワードが送信された場合、 **THEN** システムは認証を拒否し、具体的な失敗理由（「ユーザーが存在しません」等）を推測できる情報を返してはならない。
- **WHERE** パスワードを保存する場合、 **THEN** システムは必ずハッシュ化して保存しなければならない（平文保存禁止）。

### FR-02: トークン管理 (Token Management)
**User Story:** As a システム, I want 短命なアクセストークンと長寿命なリフレッシュトークンを使用したい, so that セキュリティと利便性を両立できる。

**Acceptance Criteria (EARS):**
- **WHEN** 保護された API が呼び出された場合、 **THEN** システムは JWT の署名と有効期限を検証しなければならない。
- **WHEN** アクセストークンの有効期限が切れている場合、 **THEN** システムはリフレッシュトークンを用いて新しいアクセストークンを再発行できなければならない。
- **WHEN** リフレッシュトークンが無効または失効している場合、 **THEN** システムは再ログインを要求しなければならない。

### FR-03: ロールベース認可 (RBAC)
**User Story:** As a 管理者, I want ユーザーの役割に応じて操作を制限したい, so that 不正なデータ操作を防げる。

**Acceptance Criteria (EARS):**
- **WHEN** API リクエストを受信した場合、 **THEN** システムはユーザーのロールがその操作に対して許可されているかを確認しなければならない。
- **WHEN** `Pending_Agent` ロールのユーザーが機密性の高い操作（物件登録など）を試みた場合、 **THEN** システムはアクセスを拒否しなければならない。
- **WHEN** 権限のないユーザーがアクセスした場合、 **THEN** システムは 403 Forbidden エラーを返さなければならない。

## 5. 非機能要件 (Non-Functional Requirements)
- **NFR-01 セキュリティ:** JWT の署名鍵（Secret）は環境変数から読み込み、コードベースには含めないこと。
- **NFR-02 エラーハンドリング:** 認証・認可エラーは、クライアントがハンドリングしやすい統一された形式で返却すること。
- **NFR-03 拡張性:** 将来的なロール追加や権限変更に耐えうる設計とすること。

## 6. エッジケース・制約 (Edge Cases & Constraints)
- **EC-01:** 削除されたユーザーのトークンが有効期限内である場合、リフレッシュトークンの検証時にアクセスを遮断できること。
- **EC-02:** ロールが未設定のユーザーは、デフォルトで `Customer` 相当の権限として扱う（Fail-closed）。

## 7. 要確認項目 (Open Questions)
以下の項目について、設計フェーズで詳細を決定する必要があります。

1. **リフレッシュトークンの保存場所:**
   - Cookie (HttpOnly) vs LocalStorage
   - *推奨案:* セキュリティ観点から HttpOnly Cookie を推奨するが、実装の複雑さと要件（React Frontend）の兼ね合いで判断が必要。

2. **権限の粒度:**
   - ロール単位（Role-based）で制御するか、パーミッション単位（Permission-based: `can_edit_listing` 等）まで分解するか。
   - *仮定:* 今回は `kickoff.md` の記述に基づき、ロール単位（RBAC）を基本とする。

## 8. 完了条件 (Completion Criteria)
- 認証フロー（ログイン、トークン発行、リフレッシュ）のドキュメント化
- バックエンドの認可ミドルウェアの実装
- 保護された API エンドポイントの実装例
- フロントエンドでのログインフロー実装例
- セキュリティ考慮事項のドキュメント化