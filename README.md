# real-estate

an app for agent and guest to interact with each other
// This allows demo → MVP without changing identity model

Note:
Admin role does not inherit Customer permissions implicitly.
All shared permissions must be explicitly declared in policy rules.

